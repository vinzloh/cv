---
import { SheetRenderer } from '../../components/sheet-renderer';
import Layout from '../../layouts/layout.astro';

const { id } = Astro.params;
---

<Layout>
  <SheetRenderer client:load id={id} />
</Layout>
